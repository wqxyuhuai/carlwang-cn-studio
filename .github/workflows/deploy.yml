name: Deploy Cloudflare Worker

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install dependencies
        run: npm ci
      - name: Validate application
        run: npm run lint && npm run typecheck
      - name: Check required Cloudflare credentials
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          set -euo pipefail
          CLOUDFLARE_API_TOKEN="${CLOUDFLARE_API_TOKEN//$'\r'/}"
          CLOUDFLARE_API_TOKEN="${CLOUDFLARE_API_TOKEN//$'\n'/}"

          if [ -z "$CLOUDFLARE_API_TOKEN" ]; then
            echo "Missing CLOUDFLARE_API_TOKEN in GitHub repo settings."
            exit 1
          fi

          echo "Cloudflare token is present. Wrangler will use account_id from wrangler.jsonc."
      - name: Build OpenNext bundle
        run: npx opennextjs-cloudflare build
      - name: Prefill cache and deploy Cloudflare Worker
        # OpenNext uploads the prerendered route/data cache before Wrangler switches
        # production traffic to the new Worker version. This keeps first visitors
        # off the expensive cold-render path.
        run: npx opennextjs-cloudflare deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
