# Open Questions

1. What is the final Notion database ID for `Studio Works / Cases`?
2. Which deployment target will run server-side sync actions?
3. Should contact messages be stored in Notion, email, a database, or another service?
4. What is the final admin authentication method?
5. Which social links are final?
6. Should the temporary `uploads/site-content.json` schema be replaced by split `site`, `works/index`, and `works/{slug}` JSON before launch?
7. Should videos be uploaded to OSS as MP4 only, or should HLS/transcoded variants be generated?
8. Which domain or CDN path should be used for OG images?
